tabs:
  label: Tabs
  category: 'Extra Layouts'
  template: templates/tabs
  class: '\Drupal\layout_builder_tabs\Plugin\Layout\TabsLayout'
  icon_map:
    - [first, second, third]
    - [all]
    - [all]
    - [all]
    - [all]
  regions:
    tabs:
      label: Tab Set
    # ideally this would be configurable so there could be a number of rows,
    # each one of which is a tab, that can contain multiple blocks, but this
    # would also require adding a label to each tab though configuration.
